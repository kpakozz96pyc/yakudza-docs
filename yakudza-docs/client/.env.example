# API Base URL for production builds
# Leave empty or use '/api' to use relative URLs (recommended when frontend is served by backend)
# Use absolute URL (e.g., 'http://your-domain.com/api') if frontend is served separately
VITE_API_BASE_URL=/api
